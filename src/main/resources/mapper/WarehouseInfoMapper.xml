<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.luoyuequan.mqerp.mapper.WarehouseInfoMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="cn.luoyuequan.mqerp.entity.WarehouseInfo">
        <id column="warehouse_id" property="warehouseId" />
        <result column="warehouse_number" property="warehouseNumber" />
        <result column="manager_id" property="managerId" />
        <result column="area" property="area" />
        <result column="warehouse_address" property="warehouseAddress" />
        <result column="notice_minimum" property="noticeMinimum" />
        <result column="warn_minimum" property="warnMinimum" />
        <result column="note" property="note" />
        <result column="is_deleted" property="deleted" />
        <result column="is_close" property="close" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        warehouse_id, warehouse_number, manager_id, area, warehouse_address, notice_minimum, warn_minimum, note, is_deleted, is_close, create_time, update_time
    </sql>

</mapper>
